<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk tools:overrideLibrary="com.google.android.exoplayer2.core,
            com.google.android.exoplayer2.ext.ima,
            com.google.android.exoplayer2.source.hls,
            com.google.android.exoplayer2.ui" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WebToFeedActivity" />
        <activity android:name=".FeedEntryPointActivity" />
        <activity android:name=".carousel.NativeCarouselActivity" />
        <activity android:name=".nativead2.NativeAd2Activity" />
        <activity android:name=".nativead2.carousel.NativeAd2CarouselActivity" />
        <activity android:name=".buzzbanner.BuzzBannerActivity" />

        <!-- Caution: Replace `000000000000000` with Your AppKey -->
        <meta-data
            android:name="com.buzzvil.APP_KEY"
            android:value="app-pub-100000043" />
    </application>
</manifest>
